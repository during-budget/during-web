import React from 'react';
import { CardDataType } from '../../util/api/assetAPI';

interface CardStatusProps {
  cards: CardDataType[];
}

const CardStatus = ({ cards }: CardStatusProps) => {
  return <section>CardStatus</section>;
};

export default CardStatus;
